import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#3c464b',
        },
        secondary: {
            main: '#ffb900',
        },
        success: {
            main: '#0082A4',
        },
    },
});

export default theme